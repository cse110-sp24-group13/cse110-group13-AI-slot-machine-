Refine the slot machine web app into a polished, production-quality experience using only vanilla HTML, CSS, and JavaScript. Ensure the game mechanics are fully complete and intuitive: spinning reels must animate smoothly, resolve to randomized outcomes, and clearly determine wins/losses with visible rules.

Add clear game logic transparency: display payout rules, probabilities (or implied odds), and how “temperature” affects randomness. Ensure token balance updates correctly with each spin and prevent invalid states (e.g., spinning with zero tokens).

Improve UX with responsive layout, accessible semantic HTML, and consistent visual hierarchy. Add subtle animations (reel spin easing, button states, result highlights) without overcomplicating the code.

Enhance the AI satire: system logs should dynamically reflect outcomes with varied, witty messages tied to wins, losses, and randomness.

Ensure code quality: modular JavaScript, readable structure, comments where helpful, and separation of concerns. Handle edge cases gracefully and ensure everything works reliably without external libraries.